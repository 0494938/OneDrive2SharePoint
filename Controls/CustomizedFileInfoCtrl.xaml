<UserControl x:Class="GcjUiCtrl.Control.CustomizedFileInfoCtrl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GcjUiCtrl.Control"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="160"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="20"/>
            </Grid.RowDefinitions>
            <Image Grid.Row="0" Source="{Binding Path=ThumbnailImageSource, RelativeSource={RelativeSource AncestorType=UserControl}}" Grid.RowSpan="3"></Image>
            <Grid Grid.Row="1" Visibility="{Binding Path=UserInfo,Mode=OneWay,Converter={StaticResource UserInfoToVisibilityConverter}}" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="40"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <local:CustomizedRoundImage Grid.Column="0" x:Name="imgUser" Height="40" Source="{Binding Path=UserImageSource, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                <TextBlock Grid.Column="1" x:Name="txtUserInfo" Text="User Info..." VerticalAlignment="Center"/>
            </Grid>
        </Grid>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" FontSize="16" Text="{Binding Path=FilePath, RelativeSource={RelativeSource AncestorType=UserControl}}" VerticalAlignment="Center"/>
            <TextBlock Grid.Row="1" FontSize="12" Text="{Binding Path=FileNameDesc, RelativeSource={RelativeSource AncestorType=UserControl}}" VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</UserControl>
