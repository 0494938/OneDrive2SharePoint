<UserControl x:Class="GcjUiCtrl.Control.CustomizedSideBySideDiffViewerUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GcjUiCtrl.Control"
             xmlns:Diffctrl="clr-namespace:GcjDiff.Wpf.Controls;assembly=GcjDiff.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="xCustomizedSideBySideDiffViewerUserControlStyle" TargetType="local:CustomizedSideBySideDiffViewerUserControl"/>
    </UserControl.Resources>
    <Grid>
        <Diffctrl:SideBySideDiffViewer
             Grid.Row="2"  Grid.Column="0" Grid.ColumnSpan="2" 
             AllowDrop="True" x:Name="diffViewer"
             Style="{DynamicResource xSideBySideDiffViewerStyle}" DiffModel="{Binding DiffModel, RelativeSource={RelativeSource AncestorType=UserControl}}"
            
             Background="{Binding Background, RelativeSource={RelativeSource AncestorType=UserControl}}"
             Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=UserControl}}"
             Visibility="{Binding Visibility, RelativeSource={RelativeSource AncestorType=UserControl}}"
             VerticalAlignment="{Binding VerticalAlignment, RelativeSource={RelativeSource AncestorType=UserControl}}"
             VerticalContentAlignment="{Binding VerticalContentAlignment, RelativeSource={RelativeSource AncestorType=UserControl}}"
             HorizontalAlignment="{Binding HorizontalAlignment, RelativeSource={RelativeSource AncestorType=UserControl}}"
             HorizontalContentAlignment="{Binding HorizontalContentAlignment, RelativeSource={RelativeSource AncestorType=UserControl}}"
             Width="{Binding Width, RelativeSource={RelativeSource AncestorType=UserControl}}"
             Height="{Binding Height, RelativeSource={RelativeSource AncestorType=UserControl}}"
             FontFamily="{Binding FontFamily, RelativeSource={RelativeSource AncestorType=UserControl}}"
             FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
             FontStretch="{Binding FontStretch, RelativeSource={RelativeSource AncestorType=UserControl}}"
             FontStyle="{Binding FontStyle, RelativeSource={RelativeSource AncestorType=UserControl}}"
             FontWeight="{Binding FontWeight, RelativeSource={RelativeSource AncestorType=UserControl}}"
            
             SplitterBorderThickness="1"/>
    </Grid>
</UserControl>
